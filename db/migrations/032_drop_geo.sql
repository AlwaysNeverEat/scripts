-- Пасхалка «Гео» (угадай место по снимку улицы) убрана: игра не работала —
-- держалась она на чужом банке снимков Mapillary, и без него от неё оставалось
-- пустое окно. Миграция 031 создавала её таблицы; здесь они убираются.
--
-- Данные не переносятся никуда: топы гео мерили только гео, а сами партии
-- (загаданные точки, догадки, ход дуэли) без игры бессмысленны.
--
-- Порядок важен: сначала партии, потом счёт — внешних ключей между ними нет,
-- но так читается, что чем было.
DROP TABLE IF EXISTS geo_daily;
DROP TABLE IF EXISTS geo_runs;
DROP TABLE IF EXISTS geo_duels;
DROP TABLE IF EXISTS geo_daily_scores;
DROP TABLE IF EXISTS geo_solo_scores;
DROP TABLE IF EXISTS geo_duel_scores;
